PATCH Conexão Street — Login obrigatório + UX (2026-02-19)

Arquivos incluídos (substituir no seu repo, mantendo caminhos):
- js/checkout.js
- js/pagamento.js
- js/member.js

O que muda:
1) checkout.js:
   - Dentro de insertOrder(): exige sessão (u.id). Se não estiver logado:
     -> toast + salva cs_after_login + redireciona para member.html + aborta.
   - user_id no payload vira u.id (sem null).

2) pagamento.js:
   - Antes de atualizar pedido (Já paguei), exige sessão.
     -> toast + salva cs_after_login + redireciona para member.html.

3) member.js:
   - Após login bem-sucedido: se existir cs_after_login, redireciona automaticamente de volta.

Checksums (sha1 curto):
- checkout.js: 9043e23ba1d2 -> a08ca55c253f
- pagamento.js: 8bcd57b8894b -> 0c8580d79a7e
- member.js:   49bc6065fbe8 -> 99bbe479c93d
